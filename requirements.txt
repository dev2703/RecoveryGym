pydantic>=2.0
numpy>=1.26
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
modal>=0.64
python-dotenv>=1.0
pytest>=8.0
pytest-asyncio>=0.23

# Real providers (optional locally; required on Modal GPU image)
reactor-sdk>=0.1.0
huggingface_hub>=0.23
datasets>=2.19

# SmolVLA / training (install on GPU machine or full dev env)
# torch
# lerobot[peft]
